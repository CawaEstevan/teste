@model BibliotecaApp.Application.DTOs.LivroDto
@{
    ViewData["Title"] = "Excluir Livro";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <h2 class="mb-4 text-danger">🗑️ Excluir Livro</h2>

        <div class="alert alert-warning">
            <strong>⚠️ Atenção!</strong> Você tem certeza que deseja excluir este livro?
        </div>

        <div class="card">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">Título</dt>
                    <dd class="col-sm-9">@Model.Titulo</dd>

                    <dt class="col-sm-3">Autor</dt>
                    <dd class="col-sm-9">@Model.Autor</dd>

                    <dt class="col-sm-3">ISBN</dt>
                    <dd class="col-sm-9">@Model.ISBN</dd>

                    <dt class="col-sm-3">Ano de Publicação</dt>
                    <dd class="col-sm-9">@Model.AnoPublicacao</dd>
                </dl>
            </div>
        </div>

        <form asp-action="Delete" method="post" class="mt-3">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</div>

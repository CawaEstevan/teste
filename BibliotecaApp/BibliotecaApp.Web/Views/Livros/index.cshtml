@model IEnumerable<BibliotecaApp.Application.DTOs.LivroDto>
@{
    ViewData["Title"] = "Lista de Livros";
}

<div class="row">
    <div class="col-md-12">
        <h1 class="mb-4">📚 Lista de Livros</h1>
        <p>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Adicionar Novo Livro
            </a>
        </p>

        @if (!Model.Any())
        {
            <div class="alert alert-info">
                <strong>Nenhum livro cadastrado.</strong> Clique em "Adicionar Novo Livro" para começar.
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Título</th>
                            <th>Autor</th>
                            <th>ISBN</th>
                            <th>Ano</th>
                            <th>Status</th>
                            <th>Data Cadastro</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var livro in Model)
                        {
                            <tr>
                                <td>@livro.Titulo</td>
                                <td>@livro.Autor</td>
                                <td>@livro.ISBN</td>
                                <td>@livro.AnoPublicacao</td>
                                <td>
                                    @if (livro.Disponivel)
                                    {
                                        <span class="badge bg-success">Disponível</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning text-dark">Indisponível</span>
                                    }
                                </td>
                                <td>@livro.DataCriacao.ToString("dd/MM/yyyy")</td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@livro.Id" class="btn btn-sm btn-info" title="Ver detalhes">
                                        Detalhes
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@livro.Id" class="btn btn-sm btn-warning" title="Editar">
                                        Editar
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@livro.Id" class="btn btn-sm btn-danger" title="Excluir">
                                        Excluir
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <div class="mt-3">
                <p class="text-muted">
                    <strong>Total de livros cadastrados:</strong> @Model.Count()
                </p>
            </div>
        }
    </div>
</div>